return {
  {
    "tpope/vim-fugitive",
    config = function()
      -- Any optional configuration for vim-fugitive can go here
      -- For example, you might set up key mappings specific to fugitive
    end,
  },
}
